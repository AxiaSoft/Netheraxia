# راه‌اندازی سیستم حساب کاربری و تیم‌ها

این راهنما یک‌بار انجام می‌شود و حدود ۱۰ دقیقه وقت می‌برد. بعدش همه‌چیز از پنل مدیریت کنترل می‌شود.

---

## ۱) ساخت پروژه در Supabase

۱. به [supabase.com](https://supabase.com) برو و با گیت‌هاب وارد شو (رایگان است).
۲. روی **New Project** بزن.
   - **Name:** `netheraxia`
   - **Database Password:** یک رمز قوی بگذار و **جایی یادداشت کن**.
   - **Region:** `Frankfurt (eu-central-1)` — نزدیک‌ترین به ایران و اروپا.
۳. حدود ۲ دقیقه صبر کن تا پروژه ساخته شود.

---

## ۲) ساخت جدول‌ها

۱. از منوی سمت چپ → **SQL Editor** → **New query**.
۲. کل محتوای فایل `supabase/schema.sql` را کپی کن و داخل کادر بچسبان.
۳. روی **Run** بزن (یا `Ctrl+Enter`).
۴. باید پیام **Success. No rows returned** ببینی.

> اگر خطا دیدی، کل متن خطا را بردار و بفرست. اجرای دوباره‌ی این فایل مشکلی ایجاد نمی‌کند.

---

## ۳) تنظیم ورود

۱. منوی چپ → **Authentication** → **Sign In / Providers**.
۲. مطمئن شو **Email** روشن است.
۳. گزینه‌ی **Confirm email** را:
   - **خاموش** کن → بازیکن بلافاصله بعد از ثبت‌نام وارد می‌شود (**پیشنهاد می‌شود**، چون ایمیل تأیید گاهی به اسپم می‌رود).
   - **روشن** بگذار → بازیکن باید ایمیلش را تأیید کند (امن‌تر ولی دردسر بیشتر).

### مهم — آدرس سایت را ثبت کن
منوی چپ → **Authentication** → **URL Configuration**:
- **Site URL:** `https://axiasoft.github.io/Netheraxia/`
- در **Redirect URLs** هم همین آدرس را اضافه کن.

---

## ۴) گرفتن کلیدها

۱. منوی چپ → **Project Settings** (چرخ‌دنده) → **API**.
۲. دو مقدار را کپی کن:
   - **Project URL** → چیزی شبیه `https://abcdefgh.supabase.co`
   - **anon public** (زیر Project API keys) → یک رشته‌ی بلند که با `eyJ...` شروع می‌شود

> ⚠️ کلید **service_role** را هرگز کپی نکن و هیچ‌جا نگذار. فقط `anon public`.
> کلید `anon` عمومی است و افشایش خطری ندارد — امنیت واقعی با قوانین RLS تأمین شده است.

---

## ۵) وصل کردن سایت

۱. پنل مدیریت (`admin.html`) را باز کن.
۲. تب **🧑‍🤝‍🧑 بازیکنان و تیم‌ها**.
۳. دو مقدار بالا را در کادرها بگذار.
۴. **🔍 تست اتصال** → باید پیام سبز بگیری.
۵. **💾 ذخیره و انتشار** → فایل `js/nx-config.js` روی گیت‌هاب نوشته می‌شود و سایت وصل می‌شود.

---

## ۶) ادمین کردن حساب خودت

۱. به سایت اصلی برو و مثل بقیه **ثبت‌نام کن** (با نام ماینکرفت خودت).
۲. برگرد به Supabase → **SQL Editor** → این را اجرا کن (نام خودت را جایگزین کن):

```sql
update public.profiles set is_admin = true
where lower(mc_username) = lower('نام_ماینکرفت_تو');
```

۳. حالا در پنل مدیریت، بخش **محدودیت‌ها** و **لیست بازیکنان** برایت باز می‌شود.

> نکته: پنل مدیریت از همان نشستی استفاده می‌کند که در سایت اصلی وارد شده‌ای.
> اگر بخش تنظیمات را نمی‌بینی، در سایت اصلی وارد حساب ادمین شو و بعد پنل را تازه کن.

---

## چه چیزی از کجا کنترل می‌شود

| کار | کجا |
|---|---|
| سقف تعداد تیم‌ها (پیش‌فرض ۱۰) | پنل → بازیکنان و تیم‌ها |
| سقف نفرات هر تیم (پیش‌فرض ۱۰) | پنل → بازیکنان و تیم‌ها |
| باز/بسته بودن ثبت‌نام | پنل → بازیکنان و تیم‌ها |
| باز/بسته بودن ساخت تیم | پنل → بازیکنان و تیم‌ها |
| باز/بسته بودن عضویت | پنل → بازیکنان و تیم‌ها |
| حذف تیم یا اخراج بازیکن | پنل → بازیکنان و تیم‌ها |
| مسدود کردن بازیکن | پنل → بازیکنان و تیم‌ها |
| ساخت تیم و عضویت | خودِ بازیکن‌ها در سایت اصلی |

---

## نکات امنیتی

- تمام محدودیت‌ها **سمت دیتابیس** اعمال می‌شوند. کسی نمی‌تواند با دستکاری مرورگر تیم یازدهم بسازد یا نفر یازدهم را اضافه کند.
- ایمیل بازیکنان در لیست عمومی تیم‌ها **دیده نمی‌شود** (فقط نام ماینکرفت).
- کاپیتان نمی‌تواند تیمش را ترک کند؛ اول باید کاپیتانی را واگذار کند یا تیم را حذف کند.
- بازیکن مسدودشده نمی‌تواند تیم بسازد یا عضو شود.

---

## اگر Supabase تنظیم نشده باشد

سایت **کاملاً سالم کار می‌کند** — فقط دکمه‌ی ورود و ساخت تیم نمایش داده نمی‌شود و تیم‌ها مثل قبل از فایل `data/teams.json` خوانده می‌شوند. یعنی هیچ‌چیزی خراب نمی‌شود.
