# راه‌اندازی سیستم حساب کاربری و تیم‌ها

این راهنما یک‌بار انجام می‌شود و حدود ۱۰ دقیقه وقت می‌برد. بعدش همه‌چیز از پنل مدیریت کنترل می‌شود.

---

## ۱) ساخت پروژه در Supabase

۱. به [supabase.com](https://supabase.com) برو و با گیت‌هاب وارد شو (رایگان است).
۲. روی **New Project** بزن.
   - **Name:** `netheraxia`
   - **Database Password:** یک رمز قوی بگذار و **جایی یادداشت کن**.
   - **Region:** `Frankfurt (eu-central-1)` — نزدیک‌ترین به ایران و اروپا.
۳. حدود ۲ دقیقه صبر کن تا پروژه ساخته شود.

---

## ۲) ساخت جدول‌ها

۱. از منوی سمت چپ → **SQL Editor** → **New query**.
۲. کل محتوای فایل `supabase/schema.sql` را کپی کن و داخل کادر بچسبان.
۳. روی **Run** بزن (یا `Ctrl+Enter`).
۴. باید پیام **Success. No rows returned** ببینی.

> اگر خطا دیدی، کل متن خطا را بردار و بفرست. اجرای دوباره‌ی این فایل مشکلی ایجاد نمی‌کند.

> 🔄 **اگر قبلاً این فایل را اجرا کرده‌ای:** بعد از هر به‌روزرسانی سایت، دوباره
> اجرایش کن. مثلاً قابلیت «پرچم تیم» یک ستون جدید لازم دارد که با همین اجرای
> دوباره اضافه می‌شود. اطلاعات موجود پاک نمی‌شود.

---

## ۳) تنظیم ورود

۱. منوی چپ → **Authentication** → **Sign In / Providers**.
۲. مطمئن شو **Email** روشن است.
۳. گزینه‌ی **Confirm email** را:
   - **خاموش** کن → بازیکن بلافاصله بعد از ثبت‌نام وارد می‌شود (**پیشنهاد می‌شود**، چون ایمیل تأیید گاهی به اسپم می‌رود).
   - **روشن** بگذار → بازیکن باید ایمیلش را تأیید کند (امن‌تر ولی دردسر بیشتر).

### ⚠️ مهم‌ترین قدم — آدرس سایت را ثبت کن

اگر این را انجام ندهی، **لینک بازیابی رمز ۴۰۴ می‌دهد**. علتش این است که
سوپابیس بعد از تأیید لینک، کاربر را به Site URL برمی‌گرداند و سایت ما در
زیرشاخه‌ی `/Netheraxia/` است، نه ریشه‌ی دامنه.

منوی چپ → **Authentication** → **URL Configuration**:

- **Site URL:**
  ```
  https://axiasoft.github.io/Netheraxia/
  ```
  (اسلش آخر مهم است)

- در **Redirect URLs** روی **Add URL** بزن و این را اضافه کن:
  ```
  https://axiasoft.github.io/Netheraxia/**
  ```
  آن `**` یعنی «هر آدرسی زیر این مسیر مجاز است» و همین اجازه می‌دهد لینک
  بازیابی با پارامترهایش برگردد.

---

## ۴) گرفتن Project URL و کلید

### 🚀 ساده‌ترین راه (هر دو با هم)

بالای صفحه‌ی داشبورد، دکمه‌ی سبز **Connect** را بزن.
یک پنجره باز می‌شود؛ تب **App Frameworks** را انتخاب کن. همان‌جا هر دو مقدار
کنار هم نوشته شده‌اند و کنارشان دکمه‌ی کپی هست:

```
NEXT_PUBLIC_SUPABASE_URL=https://abcdefgh.supabase.co      ← این می‌شود Project URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxxxx         ← این می‌شود کلید
```

فقط مقدارِ **بعد از مساوی** را کپی کن (خودِ `NEXT_PUBLIC_...=` را نه).

---

### 🔍 Project URL — راه دوم (از نوار آدرس مرورگر)

Project URL همیشه از روی شناسه‌ی پروژه ساخته می‌شود. وقتی داخل پروژه‌ای،
به نوار آدرس مرورگرت نگاه کن:

```
https://supabase.com/dashboard/project/abcdefghijklmnop
                                        └────── این شناسه‌ی پروژه است ──────┘
```

پس Project URL تو می‌شود:

```
https://abcdefghijklmnop.supabase.co
```

یعنی کافی است شناسه را بردازی و `.supabase.co` به آخرش بچسبانی. همین.

> Project URL در **Integrations → Data API** هم نوشته شده است.

---

### 🔑 کلید — راه دوم

منوی چپ → **Settings** (چرخ‌دنده) → **API Keys**.

⚠️ **توجه:** صفحه‌ی قدیمی `Settings → API` دیگر وجود ندارد؛ اسمش شده **API Keys**.

در آن صفحه دو نوع کلید می‌بینی — **هر دو کار می‌کنند**:

| نوع کلید | شکل ظاهری | توضیح |
|---|---|---|
| **Publishable** (جدید) | `sb_publishable_...` | پیشنهاد می‌شود |
| **anon public** (قدیمی) | رشته‌ی بلندی که با `eyJ` شروع می‌شود | هنوز کار می‌کند |

هرکدام را که دیدی بردار.

> ⛔ **هرگز** این دو را برندار: `service_role` و `sb_secret_...`
> این‌ها کلید مدیرند و تمام قوانین امنیتی را دور می‌زنند. اگر اشتباهی
> یکی از این‌ها را در پنل بگذاری، پنل جلویت را می‌گیرد و هشدار می‌دهد.

کلید `anon` / `publishable` عمومی است و قرار است داخل مرورگر باشد —
افشایش خطری ندارد، چون امنیت واقعی با قوانین RLS تأمین شده است.

---

## ۵) وصل کردن سایت

۱. پنل مدیریت (`admin.html`) را باز کن.
۲. تب **🧑‍🤝‍🧑 بازیکنان و تیم‌ها**.
۳. دو مقدار بالا را در کادرها بگذار.
۴. **🔍 تست اتصال** → باید پیام سبز بگیری.
۵. **💾 ذخیره و انتشار** → فایل `js/nx-config.js` روی گیت‌هاب نوشته می‌شود و سایت وصل می‌شود.

---

## ۶) ادمین کردن حساب خودت

۱. به سایت اصلی برو و مثل بقیه **ثبت‌نام کن** (با نام ماینکرفت خودت).
۲. برگرد به Supabase → **SQL Editor** → این را اجرا کن (نام خودت را جایگزین کن):

```sql
update public.profiles set is_admin = true
where lower(mc_username) = lower('نام_ماینکرفت_تو');
```

۳. حالا در پنل مدیریت، بخش **محدودیت‌ها** و **لیست بازیکنان** برایت باز می‌شود.

> نکته: پنل مدیریت از همان نشستی استفاده می‌کند که در سایت اصلی وارد شده‌ای.
> اگر بخش تنظیمات را نمی‌بینی، در سایت اصلی وارد حساب ادمین شو و بعد پنل را تازه کن.

---

---

## خطاهای رایج دیتابیس

### «Could not find the 'flag' column ... in the schema cache»
### «permission denied for table profiles»
### پروفایل روی «در حال بارگذاری» می‌ماند

هر سه یک راه‌حل دارند: **کل فایل `supabase/schema.sql` را دوباره در SQL Editor
اجرا کن.** اجرای دوباره بی‌خطر است و اطلاعات پاک نمی‌شود.

چرا لازم است:
- نسخه‌های جدید سایت گاهی **ستون جدید** اضافه می‌کنند (مثل `flag` برای پرچم تیم).
- بخش **GRANT** به پستگرس می‌گوید چه کسی اجازه‌ی خواندن جدول‌ها را دارد.
  پستگرس اول GRANT را چک می‌کند و بعد RLS؛ بدون آن، حتی با policy درست،
  خواندن پروفایل رد می‌شود و کارت پروفایل خالی می‌ماند.
- دستور آخر فایل (`notify pgrst, 'reload schema'`) کش سوپابیس را تازه می‌کند.

اگر بعد از اجرای دوباره باز هم خطای schema cache دیدی، در داشبورد به
**Settings → API** برو و روی **Restart server** بزن (یا چند دقیقه صبر کن).

---

## اگر لینک بازیابی رمز ۴۰۴ داد

تقریباً همیشه یعنی **URL Configuration** تنظیم نشده است. این دو را چک کن:

| تنظیم | مقدار درست |
|---|---|
| Site URL | `https://axiasoft.github.io/Netheraxia/` |
| Redirect URLs | `https://axiasoft.github.io/Netheraxia/**` |

بعد از اصلاح، یک لینک **جدید** درخواست کن؛ لینک قبلی همچنان خراب است.

نکات دیگر:
- هر لینک بازیابی **یک‌بار مصرف** است و بعد از مدتی منقضی می‌شود.
- بازیابی رمز با **ایمیل** انجام می‌شود، نه نام ماینکرفت.
- اگر ایمیل نرسید، پوشه‌ی **اسپم** را ببین.

---

## چه چیزی از کجا کنترل می‌شود

| کار | کجا |
|---|---|
| سقف تعداد تیم‌ها (پیش‌فرض ۱۰) | پنل → بازیکنان و تیم‌ها |
| سقف نفرات هر تیم (پیش‌فرض ۱۰) | پنل → بازیکنان و تیم‌ها |
| باز/بسته بودن ثبت‌نام | پنل → بازیکنان و تیم‌ها |
| باز/بسته بودن ساخت تیم | پنل → بازیکنان و تیم‌ها |
| باز/بسته بودن عضویت | پنل → بازیکنان و تیم‌ها |
| حذف تیم یا اخراج بازیکن | پنل → بازیکنان و تیم‌ها |
| مسدود کردن بازیکن | پنل → بازیکنان و تیم‌ها |
| ساخت تیم و عضویت | خودِ بازیکن‌ها در سایت اصلی |
| دیدن پروفایل و تغییر رمز | خودِ بازیکن، از منوی حساب کاربری |
| آپلود پرچم تیم | سازنده‌ی تیم، هنگام ساخت (عکس خودکار فشرده و ذخیره می‌شود) |

---

## نکات امنیتی

- تمام محدودیت‌ها **سمت دیتابیس** اعمال می‌شوند. کسی نمی‌تواند با دستکاری مرورگر تیم یازدهم بسازد یا نفر یازدهم را اضافه کند.
- ایمیل بازیکنان در لیست عمومی تیم‌ها **دیده نمی‌شود** (فقط نام ماینکرفت).
- کاپیتان نمی‌تواند تیمش را ترک کند؛ اول باید کاپیتانی را واگذار کند یا تیم را حذف کند.
- بازیکن مسدودشده نمی‌تواند تیم بسازد یا عضو شود.

---

## اگر Supabase تنظیم نشده باشد

سایت **کاملاً سالم کار می‌کند** — فقط دکمه‌ی ورود و ساخت تیم نمایش داده نمی‌شود و تیم‌ها مثل قبل از فایل `data/teams.json` خوانده می‌شوند. یعنی هیچ‌چیزی خراب نمی‌شود.
