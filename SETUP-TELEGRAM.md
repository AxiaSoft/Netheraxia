# راه‌اندازی «فقط اعضای گروه تلگرام می‌توانند ثبت‌نام کنند»

با این قابلیت، هر کسی که در سایت ثبت‌نام می‌کند اول باید ثابت کند عضو گروه
تلگرام شماست. اگر عضو نباشد، حسابش **ساخته نمی‌شود**.

> ⏱ حدود ۱۰ دقیقه وقت می‌برد و باید یک‌بار انجام شود.

---

## چرا این‌قدر مرحله دارد؟

خیلی خلاصه: **توکن ربات هرگز نباید داخل سایت باشد.**

سایت شما یک سایت ساده روی GitHub Pages است؛ یعنی هر چیزی که در فایل‌هایش
بگذاریم، هر بازدیدکننده‌ای می‌تواند ببیند. اگر توکن ربات آنجا باشد، هر کسی
می‌تواند ربات شما را کامل تصاحب کند.

از طرف دیگر، تلگرام فقط به کسی که توکن ربات را دارد می‌گوید «فلانی عضو گروه
هست یا نه». پس این سؤال باید جایی پرسیده شود که مخفی باشد → یک تابع کوچک روی
سوپابیس (Edge Function).

مسیر کار این‌طور می‌شود:

```
بازیکن → دکمه‌ی تلگرام → تابع روی سوپابیس (توکن اینجاست، مخفی)
                              ↓ از تلگرام می‌پرسد: عضو است؟
                          بله → یک «بلیت» یک‌بارمصرف صادر می‌کند
                              ↓
                        ثبت‌نام با آن بلیت انجام می‌شود
```

نکته‌ی مهم: خود دیتابیس هم دوباره بلیت را چک می‌کند. یعنی حتی اگر کسی
حرفه‌ای باشد و بخواهد مستقیم به API درخواست بفرستد و صفحه‌ی سایت را دور بزند،
باز هم بدون بلیت معتبر ثبت‌نامش رد می‌شود.

---

## مرحله ۱ — ساختن ربات

۱. در تلگرام به [@BotFather](https://t.me/BotFather) پیام بدهید
۲. دستور `/newbot` را بفرستید و یک نام و یک یوزرنیم انتخاب کنید
۳. توکنی که می‌دهد را کپی کنید؛ چیزی شبیه این است:

```
7123456789:AAF-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

> ⚠️ این توکن مثل رمز عبور است. جایی برای کسی نفرستید و در سایت نگذارید.

---

## مرحله ۲ — ربات را در گروه ادمین کنید

این مرحله را حتماً انجام دهید، وگرنه هیچ‌چیز کار نمی‌کند.

۱. ربات را به گروه اضافه کنید
۲. در تنظیمات گروه → Administrators → ربات را ادمین کنید

**چرا؟** تلگرام فقط به رباتی که ادمین گروه است اجازه می‌دهد بپرسد چه کسی عضو
است. اگر ربات ادمین نباشد، تلگرام درباره‌ی همه می‌گوید «عضو نیست» و در نتیجه
هیچ‌کس نمی‌تواند ثبت‌نام کند.

---

## مرحله ۳ — شناسه‌ی گروه را پیدا کنید

**اگر گروه عمومی است** (لینک `t.me/YourGroup` دارد):
همان `@YourGroup` را بردارید. تمام.

**اگر گروه خصوصی است:**

۱. ربات [@getidsbot](https://t.me/getidsbot) را به گروه اضافه کنید
۲. عددی که می‌دهد را بردارید — با `-100` شروع می‌شود، مثل `-1001234567890`
۳. بعد می‌توانید آن ربات را از گروه حذف کنید

---

## مرحله ۴ — نصب اسکریپت دیتابیس

۱. وارد [داشبورد سوپابیس](https://supabase.com/dashboard) شوید
۲. از منوی راست **SQL Editor** را باز کنید
۳. کل فایل `supabase/schema.sql` را کپی و اجرا کنید (**Run**)
۴. باید در آخر این پیام را ببینید:

```
✅ اسکیما کامل نصب شد
```

اگر این پیام را ندیدید یعنی فایل کامل اجرا نشده؛ دوباره از اول کپی کنید.

> این کار به فایل قبلی آسیب نمی‌زند و اطلاعات بازیکنان پاک نمی‌شود.
> فقط جدول بلیت‌ها و چند ستون جدید اضافه می‌شود.

---

## مرحله ۵ — نصب تابع بررسی روی سوپابیس

۱. در داشبورد سوپابیس → **Edge Functions**
۲. روی **Deploy a new function** → **Via Editor** بزنید
۳. اسم تابع را دقیقاً این بگذارید:

```
telegram-verify
```

۴. کل محتوای فایل `supabase/functions/telegram-verify/index.ts` را
   کپی کنید و داخل ویرایشگر بچسبانید (کد قبلی را پاک کنید)
۵. روی **Deploy** بزنید

> اسم تابع باید دقیقاً `telegram-verify` باشد، وگرنه سایت پیدایش نمی‌کند.

---

## مرحله ۶ — گذاشتن توکن در جای امن

۱. در همان صفحه‌ی Edge Functions → **Secrets**
   (یا از مسیر Project Settings → Edge Functions → Secrets)
۲. این دو مقدار را اضافه کنید:

| نام | مقدار |
|---|---|
| `TELEGRAM_BOT_TOKEN` | توکنی که BotFather داد |
| `TELEGRAM_CHAT_ID` | `@YourGroup` یا عدد `-100...` |

۳. ذخیره کنید

این تنها جایی است که توکن ذخیره می‌شود و از داخل مرورگر قابل دیدن نیست.

---

## مرحله ۷ — ثبت دامنه برای ربات

بدون این مرحله دکمه‌ی تلگرام در سایت ظاهر نمی‌شود.

۱. دوباره به [@BotFather](https://t.me/BotFather) بروید
۲. دستور `/setdomain` را بفرستید
۳. ربات خود را انتخاب کنید
۴. این آدرس را بفرستید:

```
axiasoft.github.io
```

---

## مرحله ۸ — روشن کردن از پنل مدیریت

۱. پنل مدیریت سایت را باز کنید → تب **حساب‌ها**
۲. با حساب ادمین وارد شوید
۳. گزینه‌ی **«✈️ عضویت در گروه تلگرام برای ثبت‌نام الزامی باشد»** را روشن کنید
۴. این دو کادر را پر کنید:
   - **لینک گروه تلگرام**: مثل `https://t.me/YourGroup`
   - **نام کاربری ربات**: بدون `@` — مثل `NetheraxiaBot`
۵. **ذخیره تنظیمات**

تمام. حالا فرم ثبت‌نام دو مرحله دارد و تا وقتی عضویت تأیید نشود، دکمه‌ی
«ساخت حساب» خاموش است.

---

## امتحان کردن

بهترین راه: از یک حساب تلگرام که **عضو گروه نیست** امتحان کنید.
باید این پیام را ببینید:

> ❌ شما هنوز عضو گروه تلگرام نیستید. اول در گروه عضو شوید، بعد دوباره تلاش کنید.

بعد همان حساب را عضو گروه کنید و دوباره بزنید — این بار باید تأیید شود.

---

## اگر مشکلی پیش آمد

| چه می‌بینید | یعنی چه | راه‌حل |
|---|---|---|
| دکمه‌ی تلگرام اصلاً نمی‌آید | دامنه ثبت نشده | مرحله‌ی ۷ را انجام دهید |
| «سرویس بررسی تلگرام نصب نشده» | تابع نصب نشده یا اسمش فرق دارد | مرحله‌ی ۵؛ اسم باید `telegram-verify` باشد |
| «بررسی تلگرام روی سرور پیکربندی نشده» | توکن ذخیره نشده | مرحله‌ی ۶ |
| همه «عضو نیستند» حتی اعضای واقعی | ربات در گروه ادمین نیست | مرحله‌ی ۲ |
| «جدول بلیت‌ها پیدا نشد» | اسکیما اجرا نشده | مرحله‌ی ۴ |
| «با این حساب تلگرام قبلاً ثبت‌نام شده» | هر تلگرام فقط یک حساب | این عمدی است (ضد چند‌حسابه) |
| «زمان تأیید تلگرام تمام شده» | بیش از ۳۰ دقیقه معطل شده | دوباره روی دکمه‌ی تأیید بزنید |

### خاموش کردن موقت

اگر خواستید سریع همه‌چیز را به حالت قبل برگردانید، کافی است در پنل مدیریت
تیک **«عضویت در گروه تلگرام الزامی باشد»** را بردارید و ذخیره کنید. ثبت‌نام
بلافاصله مثل قبل باز می‌شود.

---

## چند نکته‌ی امنیتی

- توکن ربات فقط در Secrets سوپابیس است؛ نه در سایت، نه در گیت‌هاب، نه در
  پنل مدیریت.
- دیتای دکمه‌ی تلگرام با امضای دیجیتال چک می‌شود، پس کسی نمی‌تواند خودش را
  جای شخص دیگری جا بزند.
- هر بلیت فقط **یک بار** و تا **۳۰ دقیقه** قابل استفاده است.
- هر حساب تلگرام فقط می‌تواند **یک** حساب بازی بسازد.
- جدول بلیت‌ها از دسترس مرورگر خارج است؛ نه خواندنی است نه نوشتنی.
